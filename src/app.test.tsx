import '@testing-library/jest-dom';
describe('Testing <App /> component', () => {
  test('1 + 1 = 2', async () => {
    expect(1 + 2).toBe(3);
  });

  test('1 + 1 = 3', async () => {
    expect(1 + 3).toBe(4);
  });
});
